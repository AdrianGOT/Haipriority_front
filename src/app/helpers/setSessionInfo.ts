import { LoginResponse } from "../../interfaces/client.interfaces";

export const setInfo = (clientInfo: LoginResponse)  => {
    console.log(clientInfo);
    

    // Hacer lo propio con el estado global.

} 